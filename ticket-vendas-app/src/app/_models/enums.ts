export enum TypeItem {
    vehicle = 0,
    people = 1,
    pet = 2,
}

export enum StatusEnum {
    found = 0,
    lost = 1
}

export enum TypeUser {
    admin = 99,
    user = 1
}

export enum SetorEnum {
    PISTA = 1,
    CAMAROTE = 2
}